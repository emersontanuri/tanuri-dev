# Source material for the site, kept local by the owner's choice.
# Never committed and never published; the site's content collections are the
# single source of truth once migration is complete.
my-data.md

# Dependencies
node_modules

# Nuxt build output and local state
.nuxt
.output
.data
dist

# Logs
*.log
